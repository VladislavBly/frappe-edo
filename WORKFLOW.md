# Описание флоу работы системы EDO (Электронный документооборот)

## Обзор системы

Система EDO реализует процесс согласования и исполнения документов с четким разделением ролей и контролем доступа.

---

## Роли пользователей

### 1. **EDO Manager (Менеджер)**
- **Права**: Создание документов, просмотр всех документов
- **Видимость**: Видит все документы в системе
- **Действия**: Создает документы, назначает исполнителей

### 2. **EDO Director (Директор)**
- **Права**: Согласование/отказ документов, просмотр всех документов
- **Видимость**: Видит все документы в системе
- **Действия**: Согласовывает или отклоняет документы, созданные менеджером

### 3. **EDO Executor (Исполнитель)**
- **Права**: Подписание документов, просмотр только своих документов
- **Видимость**: Видит только документы, где он указан как исполнитель или соисполнитель
- **Действия**: Подписывает документы, которые ему назначены

### 4. **EDO Admin (Администратор)**
- **Права**: Полный доступ ко всем функциям системы
- **Видимость**: Видит все документы
- **Действия**: Может выполнять действия любой роли

---

## Статусы документов

1. **Новый** - Документ только что создан менеджером
2. **Согласован** - Директор согласовал документ (если нет исполнителей)
3. **Отказан** - Директор отклонил документ
4. **На исполнении** - Документ согласован и отправлен исполнителям
5. **Выполнено** - Все исполнители подписали документ
6. **Архив** - Документ архивирован

---

## Полный флоу работы системы

### Этап 1: Создание документа (Менеджер)

```
Менеджер создает документ
    ↓
Заполняет обязательные поля:
  - Название документа
  - Корреспондент
  - Тип документа
  - Исполнитель (обязательно для дальнейшего процесса)
  - Соисполнители (опционально)
  - Краткое содержание
  - Приоритет
  - Классификация
    ↓
Система автоматически устанавливает статус: "Новый"
    ↓
Документ сохраняется в базе данных
```

**Результат**: Документ со статусом "Новый" появляется у менеджера и директора.

---

### Этап 2: Согласование директором

#### Вариант 2.1: Согласование

```
Директор видит документ со статусом "Новый"
    ↓
Открывает документ, просматривает содержимое
    ↓
Нажимает кнопку "Согласовать"
    ↓
Может добавить комментарий (опционально)
    ↓
Подтверждает согласование
    ↓
Система обновляет документ:
  - director_approved = True
  - director_rejected = False
  - director_user = текущий пользователь
  - director_decision_date = текущая дата/время
  - director_comment = комментарий (если указан)
    ↓
Проверка: Есть ли исполнитель?
    ↓
  ├─ ДА → Статус меняется на "На исполнении"
  │        Документ появляется у исполнителей
  │
  └─ НЕТ → Статус меняется на "Согласован"
           Процесс завершен
```

#### Вариант 2.2: Отказ

```
Директор видит документ со статусом "Новый"
    ↓
Открывает документ, просматривает содержимое
    ↓
Нажимает кнопку "Отказать"
    ↓
Обязательно указывает причину отказа
    ↓
Подтверждает отказ
    ↓
Система обновляет документ:
  - director_approved = False
  - director_rejected = True
  - director_user = текущий пользователь
  - director_decision_date = текущая дата/время
  - director_comment = причина отказа
  - status = "Отказан"
    ↓
Документ НЕ доходит до исполнителей
Процесс завершен (документ отклонен)
```

**Результат**: 
- При согласовании → документ переходит в статус "На исполнении" или "Согласован"
- При отказе → документ переходит в статус "Отказан" и процесс останавливается

---

### Этап 3: Подписание исполнителями (только если статус "На исполнении")

```
Исполнитель видит документ в своем списке
(только если он указан как executor или co_executor)
    ↓
Открывает документ, просматривает содержимое
    ↓
Видит вкладку "Подписи" с прогрессом:
  "Подписи (X/Y)" где X - подписанные, Y - всего исполнителей
    ↓
Нажимает кнопку "Подписать документ"
    ↓
Может добавить комментарий (опционально)
    ↓
Подтверждает подписание
    ↓
Система добавляет запись в таблицу signatures:
  - user = текущий пользователь
  - signed_at = текущая дата/время
  - comment = комментарий (если указан)
    ↓
Система проверяет: Все ли исполнители подписали?
    ↓
  ├─ НЕТ → Документ остается в статусе "На исполнении"
  │        Прогресс обновляется: "Подписи (X+1/Y)"
  │
  └─ ДА → Статус автоматически меняется на "Выполнено"
          Процесс завершен успешно
```

**Результат**: 
- После подписания всех исполнителей → документ переходит в статус "Выполнено"
- Процесс согласования и исполнения завершен

---

## Визуальное отображение процесса

### Progress Bar (Прогресс согласования)

Система отображает визуальный прогресс-бар с этапами:

1. ✅ **Создан менеджером** (всегда выполнен)
   - Дата создания документа

2. ⏳/✅ **На согласовании у директора** / **Согласован директором**
   - Показывается как "pending" если статус "Новый"
   - Показывается как "completed" если директор согласовал
   - Дата решения директора

3. ❌ **Отказан директором** (если отклонен)
   - Дата отказа
   - Процесс останавливается здесь

4. ⏳/✅ **Подписание исполнителями (X/Y)**
   - Показывается только если статус "На исполнении" или "Выполнено"
   - Отображает прогресс: сколько из всех исполнителей подписали
   - Показывается как "completed" когда все подписали

5. ✅ **Выполнено** (финальный этап)
   - Дата завершения

---

## Фильтрация документов по ролям

### Менеджер и Директор
```
Видимость: ВСЕ документы в системе
  - Статус "Новый"
  - Статус "Согласован"
  - Статус "Отказан"
  - Статус "На исполнении"
  - Статус "Выполнено"
  - Статус "Архив"
```

### Исполнитель
```
Видимость: ТОЛЬКО документы, где он указан как:
  - executor (основной исполнитель)
  - co_executor (соисполнитель)

Видит документы только в статусах:
  - "На исполнении" (может подписать)
  - "Выполнено" (уже подписан)
```

**Важно**: Если документ в статусе "Новый" или "Отказан", исполнитель его НЕ видит.

---

## Вкладки в интерфейсе документа

### 1. Вкладка "Документ"
- Основная информация о документе
- Краткое содержание
- Тип документа
- Список исполнителей и соисполнителей
- Комментарий директора (если есть)
- Метаданные (даты, ID и т.д.)

### 2. Вкладка "Подписи (X/Y)"
- Progress bar с процентом подписания
- Список всех исполнителей:
  - ✅ Подписанные (с датой и комментарием)
  - ⏳ Ожидающие подписания
- Аватары пользователей
- Информация о времени подписания

### 3. Вкладка "История"
- Хронология всех этапов процесса
- Даты выполнения каждого этапа
- Статусы на каждом этапе

---

## Кнопки действий

### Для директора (когда статус "Новый")
- **"Согласовать"** - зеленная кнопка с иконкой галочки
- **"Отказать"** - красная кнопка с иконкой крестика

### Для исполнителя (когда статус "На исполнении")
- **"Подписать документ"** - синяя кнопка с иконкой ручки
- Кнопка появляется только если пользователь еще не подписал

---

## Автоматические переходы статусов

```
"Новый"
  ↓ (директор согласовал)
"Согласован" (если нет исполнителей)
  ИЛИ
"На исполнении" (если есть исполнители)
  ↓ (все исполнители подписали)
"Выполнено"
```

```
"Новый"
  ↓ (директор отказал)
"Отказан" (процесс остановлен)
```

---

## Пример полного цикла

1. **Менеджер** создает документ "Договор с поставщиком"
   - Назначает исполнителя: Иван Иванов
   - Назначает соисполнителя: Петр Петров
   - Статус: **"Новый"**

2. **Директор** видит документ в списке
   - Открывает документ
   - Просматривает содержимое
   - Нажимает "Согласовать" с комментарием "Одобрено"
   - Статус: **"На исполнении"**

3. **Иван Иванов** (исполнитель) видит документ в своем списке
   - Открывает документ
   - Видит прогресс: "Подписи (0/2)"
   - Нажимает "Подписать документ"
   - Статус: **"На исполнении"** (прогресс: 1/2)

4. **Петр Петров** (соисполнитель) видит документ в своем списке
   - Открывает документ
   - Видит прогресс: "Подписи (1/2)"
   - Нажимает "Подписать документ"
   - Статус автоматически меняется на **"Выполнено"** (прогресс: 2/2)

5. Все участники видят документ в статусе **"Выполнено"**
   - Progress bar показывает все этапы как завершенные
   - В истории видна полная хронология

---

## Важные особенности

1. **Контроль доступа**: Исполнители видят только свои документы
2. **Автоматизация**: Переход в статус "Выполнено" происходит автоматически
3. **Прозрачность**: Все этапы видны в progress bar и истории
4. **Безопасность**: Отказанные документы не доходят до исполнителей
5. **Гибкость**: Можно добавить комментарии на каждом этапе

---

## Технические детали

- **Backend**: Python (Frappe Framework)
- **Frontend**: React + TypeScript + Tailwind CSS
- **База данных**: MySQL/MariaDB
- **API**: RESTful API через Frappe
- **Аутентификация**: Frappe Session Management

---

**Система готова к использованию!** 🚀
